# Example project-scoped Codex configuration
# Save as .codex/config.toml if you want the project to advertise the MCP server.

[mcp_servers.project_dev]
url = "http://127.0.0.1:8787/mcp"
enabled = false
required = false
startup_timeout_sec = 20
tool_timeout_sec = 120
