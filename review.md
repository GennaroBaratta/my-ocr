layout review page bugs:
  - select a box and pressing delete/clicking remove scrolls the page to the top
  - moving a box scrolls the page to the top
  - add box should permit the user to create the box on page by clicking and dragging, instead of always creating the box directly on the page